spring:
  application:
    name: eureka-server
  profiles:
    active: local
  cloud:
    config:
      uri: http://localhost:8080 # Config Server 접속 정보
    loadbalancer:
      ribbon:
        enabled: false
