server:
  port: 5001
spring:
  application:
    name: item-service
  profiles:
    active: local
  cloud:
    config:
      uri: http://localhost:8080 # Config Server 접속 정보
    loadbalancer:
      ribbon:
        enabled: false